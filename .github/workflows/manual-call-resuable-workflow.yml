name: reusable workflow caller

on:
  workflow_dispatch:
  
jobs: 
  call-workflow-passing-data:
    uses: dmedeiros-veracode/veracode-sample/.github/workflows/workflow-B.yml@main
    with:
      username: mona
    secrets:
      veracode-api-id: ${{ secrets.veracode-api-id }}
